{
  "name": "byku-publikacje-mobile",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "php -S 127.0.0.1:8903 -t . tests/router.php"
  }
}
